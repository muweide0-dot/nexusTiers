{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "unzip -oq railway-source.zip -d /tmp/nexustiers-source && cp -a /tmp/nexustiers-source/nexustiers-railway/. . && corepack enable && pnpm install --frozen-lockfile && pnpm run build:railway"
  },
  "deploy": {
    "startCommand": "pnpm run start:railway",
    "healthcheckPath": "/api/healthz",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}